# RoyalJellyPhysics

cmake_minimum_required(VERSION 3.7)

project(
	RJPhysics
	DESCRIPTION "Fixed-point minimal physics for 2D fighting games"
)
set(CMAKE_CXX_STANDARD 20)

# we define MY_SOURCES to be a list of all the source files for my project
file(GLOB_RECURSE MY_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp")

add_library(RJPhysics STATIC ${MY_SOURCES})

target_include_directories(RJPhysics PUBLIC 
	"${CMAKE_CURRENT_SOURCE_DIR}/dependencies/"
	"${CMAKE_CURRENT_SOURCE_DIR}/headers/"
)